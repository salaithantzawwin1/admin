# =============================================================
# AMS — Production environment (Real Production Server)
# Deploy out-of-band; NEVER commit the real .env.prod to Git.
# =============================================================
APP_ENV=production
APP_VERSION=1.0.0

POSTGRES_DB=ams
POSTGRES_USER=ams
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD

JWT_SECRET=CHANGE_ME_STRONG_JWT_SECRET
JWT_EXPIRES_IN=8h

SEED_PASSWORD=CHANGE_ME_STRONG_SEED_PASSWORD
