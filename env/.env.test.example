# =============================================================
# AMS — Testing environment (Testing Server)
# Copy to .env.test and adjust secrets. Never commit real secrets.
# =============================================================
APP_ENV=testing
APP_VERSION=0.1.0

POSTGRES_DB=ams
POSTGRES_USER=ams
POSTGRES_PASSWORD=change_me_testing

JWT_SECRET=change_me_testing_jwt_secret
JWT_EXPIRES_IN=8h

# Default password for seeded users (admin, heads, employee)
SEED_PASSWORD=ChangeMe#2026
